import { ChangeDetectionStrategy, Component, computed, inject, signal } from '@angular/core';import { BattleFormat } from '../../core/models';import { StoreService } from '../../core/store.service';import { TeamViewComponent } from '../../shared/team-view.component';
@Component({selector:'app-saved-teams',imports:[TeamViewComponent],templateUrl:'./saved-teams.component.html',styleUrl:'./saved-teams.component.scss',changeDetection:ChangeDetectionStrategy.OnPush})export class SavedTeamsComponent{readonly store=inject(StoreService);readonly tab=signal<BattleFormat>('singolo');readonly teams=computed(()=>this.store.teams().filter(t=>t.format===this.tab()));}